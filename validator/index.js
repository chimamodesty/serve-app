const {carDataSchema, bookschema } = require("./cars/schema")
const {handymanDataSchema  } = require("./handyman/schema")
const {artistsDataSchema } = require("./artists/schema")


module.exports = {
    carDataSchema, 
    bookschema,
    handymanDataSchema,
    artistsDataSchema
}