const {carDataSchema, bookschema } = require("./cars/schema")
const {handymanDataSchema  } = require("./handyman/schema")


module.exports = {
    carDataSchema, 
    bookschema,
    handymanDataSchema
}